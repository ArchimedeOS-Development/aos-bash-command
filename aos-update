#!/bin/bash

# Couleurs
RED="\e[31m"
GREEN="\e[32m"
YELLOW="\e[33m"
CYAN="\e[36m"
BOLD="\e[1m"
RESET="\e[0m"

clear
echo -e "${CYAN}${BOLD}🔄 ArchimedeOS - Mise à jour système${RESET}\n"

# Étape 1 : mise à jour via pacman
echo -e "${YELLOW}➡ Mise à jour des paquets officiels (pacman)...${RESET}"
sudo pacman -Syu

# Étape 2 : mise à jour via AUR helper
if command -v yay &>/dev/null; then
    echo -e "\n${YELLOW}➡ Mise à jour des paquets AUR (yay)...${RESET}"
    yay -Syu --noconfirm
elif command -v paru &>/dev/null; then
    echo -e "\n${YELLOW}➡ Mise à jour des paquets AUR (paru)...${RESET}"
    paru -Syu --noconfirm
else
    echo -e "\n${RED}❌ Aucun helper AUR trouvé (yay ou paru manquant).${RESET}"
fi

# Fin
echo -e "\n${GREEN}${BOLD}✅ Système ArchimedeOS à jour !${RESET}"
